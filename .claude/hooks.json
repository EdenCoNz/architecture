{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "description": "Hook configuration for Claude Code slash commands. Hooks trigger automatically when commands complete successfully.",
  "version": "1.0.0",
  "hooks": {
    "feature": {
      "enabled": true,
      "description": "Hooks that trigger when the /feature command completes successfully",
      "onComplete": [
        {
          "command": "/push",
          "description": "Automatically commit and push the feature implementation",
          "enabled": true,
          "arguments": {
            "message": "{{IntendedCommitMessage}}"
          },
          "condition": {
            "requireAllVariables": ["FeatureID", "FeatureBranch", "IntendedCommitMessage"]
          }
        }
      ]
    }
  },
  "variables": {
    "description": "Variable placeholders that can be used in hook arguments",
    "supported": [
      {
        "name": "{{FeatureID}}",
        "description": "The numeric ID of the feature (e.g., 5)",
        "availableIn": ["feature"]
      },
      {
        "name": "{{FeatureBranch}}",
        "description": "The name of the feature branch (e.g., feature/5-user-authentication)",
        "availableIn": ["feature"]
      },
      {
        "name": "{{IntendedCommitMessage}}",
        "description": "The commit message for the feature (e.g., 'Feature 5: User Authentication System')",
        "availableIn": ["feature"]
      }
    ]
  },
  "configuration": {
    "disableAllHooks": false,
    "verbose": true,
    "failOnHookError": false
  }
}
