# =============================================================================
# Frontend Application - Environment Configuration Template
# =============================================================================
# This file contains all available configuration variables for the frontend.
# Copy this file to create environment-specific configurations:
#
#   Local Development:  .env.local
#   Staging:            .env.staging
#   Production:         .env.production
#
# IMPORTANT SECURITY NOTES:
# - Never commit .env.local, .env.staging, or .env.production to version control
# - All frontend environment variables must be prefixed with VITE_
# - Frontend env variables are embedded in the build and visible in client code
# - Never store sensitive secrets (API keys, passwords) in frontend env variables
# - Use backend API endpoints to securely access sensitive data
#
# See docs/FRONTEND_CONFIGURATION.md for detailed documentation
# =============================================================================

# -----------------------------------------------------------------------------
# Environment Settings
# -----------------------------------------------------------------------------

# NODE_ENV (set automatically by Vite, but can be overridden)
# Options: development, production, test
# This affects Vite's build mode and optimization settings
VITE_NODE_ENV=development

# -----------------------------------------------------------------------------
# API Configuration (REQUIRED)
# -----------------------------------------------------------------------------

# API_URL (REQUIRED)
# Base URL for the backend API
# Development: http://localhost:8000
# Staging:     https://api-staging.example.com
# Production:  https://api.example.com
VITE_API_URL=http://localhost:8000

# API_TIMEOUT (optional, default: 30000)
# API request timeout in milliseconds
# Valid range: 1000-120000 (1 second to 2 minutes)
VITE_API_TIMEOUT=30000

# API_ENABLE_LOGGING (optional, default: true in dev, false in prod)
# Enable detailed logging of API requests and responses
# Set to true for debugging, false in production for performance
VITE_API_ENABLE_LOGGING=true

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------

# APP_NAME (optional, default: Frontend Application)
# Application name used in UI and logging
VITE_APP_NAME=Frontend Application

# APP_VERSION (optional, default: 1.0.0)
# Application version (typically set by CI/CD from git tag)
VITE_APP_VERSION=1.0.0

# APP_TITLE (optional, default: Frontend Application)
# Browser tab title
VITE_APP_TITLE=Frontend Application

# DEBUG (optional, default: true in dev, false in prod)
# Enable debug mode features (verbose logging, React DevTools integration)
VITE_DEBUG=true

# -----------------------------------------------------------------------------
# Feature Flags
# -----------------------------------------------------------------------------

# ENABLE_ANALYTICS (optional, default: false in dev, true in prod)
# Enable analytics tracking (Google Analytics, Mixpanel, etc.)
# Should be false in development to avoid polluting analytics data
VITE_ENABLE_ANALYTICS=false

# ENABLE_ERROR_REPORTING (optional, default: false in dev, true in prod)
# Enable error reporting service (Sentry, Rollbar, etc.)
# Should be false in development unless testing error reporting
VITE_ENABLE_ERROR_REPORTING=false

# ENABLE_SERVICE_WORKER (optional, default: false)
# Enable service worker for offline support and caching
# Only enable if you have implemented a service worker
VITE_ENABLE_SERVICE_WORKER=false

# -----------------------------------------------------------------------------
# Security Settings
# -----------------------------------------------------------------------------

# SECURITY_ENABLE_CSP (optional, default: false in dev, true in prod)
# Enable Content Security Policy headers
# Should be true in production for enhanced security
VITE_SECURITY_ENABLE_CSP=false

# SECURITY_MAX_LOGIN_ATTEMPTS (optional, default: 5)
# Maximum login attempts before account lockout
# Valid range: 1-20
VITE_SECURITY_MAX_LOGIN_ATTEMPTS=5

# -----------------------------------------------------------------------------
# Third-Party Services (if applicable)
# -----------------------------------------------------------------------------

# Note: These are examples for common third-party services
# Only include keys for services you actually use
# Remember: These values are VISIBLE in client-side code

# Example: Google Analytics
# VITE_GOOGLE_ANALYTICS_ID=UA-XXXXXXXXX-X

# Example: Sentry (Error Reporting)
# VITE_SENTRY_DSN=https://xxxxx@sentry.io/xxxxx

# Example: Mapbox (if using maps)
# VITE_MAPBOX_ACCESS_TOKEN=pk.xxxxx

# -----------------------------------------------------------------------------
# Build Configuration
# -----------------------------------------------------------------------------

# These are typically set by CI/CD pipelines, not in .env files

# BUILD_TIMESTAMP (set by build process)
# VITE_BUILD_TIMESTAMP=

# GIT_COMMIT_SHA (set by CI/CD)
# VITE_GIT_COMMIT_SHA=

# GIT_BRANCH (set by CI/CD)
# VITE_GIT_BRANCH=
