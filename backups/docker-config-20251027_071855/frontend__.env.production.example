# =============================================================================
# Production Environment Configuration
# =============================================================================
# This file provides a starting point for production environment configuration.
#
# Setup Instructions:
#   1. Copy this file to .env.production
#   2. Update values for your production environment
#   3. Never commit .env.production to version control
#   4. Store production secrets in CI/CD secrets management
#   5. Use this in CI/CD: vite build --mode production
#
# Production configuration should:
# - Use HTTPS API endpoints only
# - Disable debug features
# - Enable analytics and error reporting
# - Enforce strict security settings
# =============================================================================

# Environment
VITE_NODE_ENV=production

# API Configuration (production backend - MUST use HTTPS)
VITE_API_URL=https://api.example.com
VITE_API_TIMEOUT=30000
VITE_API_ENABLE_LOGGING=false

# Application Settings
VITE_APP_NAME=Frontend Application
VITE_APP_VERSION=1.0.0
VITE_APP_TITLE=Frontend Application
VITE_DEBUG=false

# Feature Flags (enable production features)
VITE_ENABLE_ANALYTICS=true
VITE_ENABLE_ERROR_REPORTING=true
VITE_ENABLE_SERVICE_WORKER=true

# Security Settings (strict security for production)
VITE_SECURITY_ENABLE_CSP=true
VITE_SECURITY_MAX_LOGIN_ATTEMPTS=5

# Third-Party Services (production projects)
# IMPORTANT: Set these via CI/CD environment variables, not committed files
# VITE_GOOGLE_ANALYTICS_ID=UA-XXXXXXXXX-X
# VITE_SENTRY_DSN=https://xxxxx@sentry.io/xxxxx
# VITE_MAPBOX_ACCESS_TOKEN=pk.xxxxx

# Build Information (set by CI/CD pipeline)
# VITE_BUILD_TIMESTAMP=2025-10-24T12:00:00Z
# VITE_GIT_COMMIT_SHA=abc123def456
# VITE_GIT_BRANCH=main
