# =============================================================================
# Staging Environment Configuration
# =============================================================================
# This file provides a starting point for staging environment configuration.
#
# Setup Instructions:
#   1. Copy this file to .env.staging
#   2. Update values for your staging environment
#   3. Never commit .env.staging to version control
#   4. Use this in CI/CD: vite build --mode staging
#
# Staging should closely mirror production but with:
# - Separate API endpoints
# - Debug features enabled for testing
# - Separate analytics/error tracking projects
# =============================================================================

# Environment
VITE_NODE_ENV=staging

# API Configuration (staging backend)
VITE_API_URL=https://api-staging.example.com
VITE_API_TIMEOUT=30000
VITE_API_ENABLE_LOGGING=true

# Application Settings
VITE_APP_NAME=Frontend Application (Staging)
VITE_APP_VERSION=1.0.0-staging
VITE_APP_TITLE=Frontend App - Staging
VITE_DEBUG=true

# Feature Flags (test production features in staging)
VITE_ENABLE_ANALYTICS=true
VITE_ENABLE_ERROR_REPORTING=true
VITE_ENABLE_SERVICE_WORKER=false

# Security Settings (production-like security)
VITE_SECURITY_ENABLE_CSP=true
VITE_SECURITY_MAX_LOGIN_ATTEMPTS=5

# Third-Party Services (staging/test projects)
# VITE_GOOGLE_ANALYTICS_ID=UA-XXXXXXXXX-X (staging property)
# VITE_SENTRY_DSN=https://xxxxx@sentry.io/xxxxx (staging project)
